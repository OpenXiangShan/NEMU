menu "Memory Configuration"

config MBASE
  hex "Physical memory base address"
  default 0x80000000

config MSIZE
  hex "Physical memory size"
  default 0x8000000

config PC_RESET_OFFSET
  hex "Offset of reset vecotr from the base of physical memory"
  default 0x100000

endmenu #MEMORY
